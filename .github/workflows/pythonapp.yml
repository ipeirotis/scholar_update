# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Python application

on:

  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]


jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up Python 3.8
      uses: actions/setup-python@v4.5.0
      with:
        python-version: 3.8
        
    - name: Install dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip3 install flake8
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - id: 'auth'
      uses: 'google-github-actions/auth@v1'
      with:
          project_id: scholar-pub-data
          credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: 'Set up Cloud SDK'
      uses: google-github-actions/setup-gcloud@v1.1.0

    - name: Deploy Cloud Function
      run: |
        gcloud functions deploy scholar-update --project scholar-pub-data --gen2 --entry-point update_scholar_profile --runtime python38 --trigger-http --allow-unauthenticated  

    #- name: Cloud Functions Deploy
      # uses: google-github-actions/deploy-cloud-functions@v1
      # with:
        # name: scholar-update
        # project_id: scholar-pub-data
        # entry_point: update_scholar_profile 
        # runtime: python38


